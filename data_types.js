"use strict"; //treat all js code as newer version

//alert(3+3) // works in browser but not in node
let name="anu"//string(double string or single string)
let age=18//number(2 to power 53) also bigint
let isloggedin=false//boolean
let state;//undefined
let temp= null//null(typeof null=object)
